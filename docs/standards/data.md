# Data Standards

## Artifact files
- Intent artifacts live at `docs/intents/<slug>/intent.md`; tracker review records live outside the repo artifact tree, one file per Intent with accumulating rounds. Why: repo stores artifacts, tracker stores process records.
